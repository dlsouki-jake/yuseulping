# PRD: 유슬핑 (유튜브 슬라이드 브리핑)

> 기반 문서: `DESIGN.md` (아키텍처/기술스택/의사결정 히스토리는 그쪽이 원본, 여기서는 제품 요구사항 관점으로 재정리)

## 1. 개요
구독 중인 유튜브 채널에 새 롱폼 영상이 올라오면, NotebookLM으로 슬라이드 요약본(PDF)을 자동 생성하고, 사용자가 지정한 시각에 이메일로 자동 발송하는 개인용 자가호스팅 도구. 서비스명은 **유슬핑**.

## 2. 배경 / 문제 정의
- 구독 채널이 많으면 매번 영상을 직접 보고 핵심을 파악하는 데 시간이 오래 걸림
- NotebookLM이 좋은 슬라이드 요약을 만들어주지만, "영상 확인 → NotebookLM에 직접 넣기 → 슬라이드 생성 → 받아보기" 과정이 전부 수동
- 이 반복 작업을 자동화해서, 신경 쓰지 않아도 새 영상이 올라오면 요약본이 알아서 도착하게 만들고 싶음

## 3. 목표 (Goals)
- 등록한 채널의 신규 롱폼 업로드를 자동 감지 (쇼츠 제외)
- NotebookLM 슬라이드 요약을 사람 개입 없이 생성
- 결과물을 사용자가 지정한 시각에 하루 1회 "오늘의 요약"으로 묶어 이메일로 자동 수령
- 서버 비용 0원, 전부 무료 도구/무료 티어로 동작
- **구글 심사(verification)나 결제, Cloud Console 설정이 전혀 없이, 누구나 무료로 설치해 쓸 수 있게** 오픈소스로 공개
- 설정은 터미널이 아닌 로컬 웹 UI로 진행, 개발 지식 없는 사람도 설치 가능

## 4. 타겟 사용자
- 1차: 제작자 본인 (개발 지식 없음, 본인의 구독 채널 요약을 매일 자동으로 받고 싶음)
- 2차: GitHub에서 이 리포를 발견한 비개발자~초급 개발자 개인 사용자 (각자 자기 계정으로 셀프호스팅)
- 명시적으로 **아닌 대상**: 여러 사용자를 한 서버에서 관리하는 운영자, 기업 고객

## 5. 사용자 시나리오
1. 사용자가 리포를 다운로드하고 프로그램을 실행하면 로컬 웹 UI가 브라우저에 열린다
2. 요약받고 싶은 유튜브 채널의 URL을 직접 붙여넣어 등록한다 (구글 로그인 불필요, 최대 5개 이내 등록 권장)
3. 받을 이메일 주소와 Gmail 앱 비밀번호(구글 계정 보안 설정에서 발급, Cloud Console 아님)를 입력한다
4. 슬라이드 요약을 받아볼 시각(오전 6시~오후 6시 중 1시간 단위 선택, 기본 오전 8:00)을 지정한다
5. 이후 아무것도 하지 않아도, 등록한 채널에 새 롱폼 영상이 올라오면 자동으로 슬라이드 요약이 생성되고, 지정한 시각에 그날 만들어진 요약들이 "오늘의 요약"으로 묶여 이메일로 도착한다 (PDF 첨부)

## 6. 기능 요구사항

**FR1. 채널 등록**
- 사용자가 유튜브 채널 URL을 직접 입력해 등록 (구글 로그인·구독목록 자동조회 불필요)
- 등록된 채널은 로컬 설정 파일에 저장되어 재실행 시 유지, 언제든 추가/삭제 가능
- 채널 등록 UI에는 현재 등록 개수와 권장 개수(FR7 참고)가 함께 표시됨

**FR2. 신규 업로드 감지**
- 등록된 채널의 최근 업로드를 주기적으로(기본 1시간, 설정 가능) yt-dlp로 확인 (RSS 피드는 서비스 종료가 확인되어 미사용, DESIGN.md 참고)
- 정확한 시각이 아니라 "마지막 확인 후 1시간이 지났으면 지금 확인"하는 **따라잡기 방식** — PC가 항상 켜져 있지 않아도 됨 (FR10 참고)
- 이미 처리한 영상은 로컬에 기록해 중복 처리 방지 (구현·실제 채널 테스트 완료)
- 신규 영상 발견 시 **`yt-dlp` 라이브러리**로 재생시간(duration)을 조회해, 기본 180초(설정 가능) 이하면 쇼츠로 간주해 처리 대상에서 제외 (롱폼만 처리). API 키·OAuth·Cloud Console 설정 불필요

**FR3. 슬라이드 요약 생성**
- 신규 영상 URL을 NotebookLM에 소스로 추가 (notebooklm-py 경유)
- Slide Deck 생성 요청 → 완료 대기 → PDF로 다운로드
- 자막이 아직 없어 생성 실패 시, 즉시 포기하지 않고 다음 폴링 주기(1시간 후)에 1회 재시도. 재시도에도 실패하면 최종 실패로 기록하고 파이프라인은 계속 진행 (전체가 멈추지 않음)
- NotebookLM 무료 요금제의 일일 생성 한도(정확한 슬라이드덱 한도는 비공개, 실측 필요)를 초과하면 대기열 정책(FR4)에 따라 처리

**FR4. 일일 처리 한도 초과 시 대기열 정책 (설정 가능)**
- **옵션 A (이월/대기, 기본값)**: 당일 처리 못 한 영상은 대기열에 쌓아뒀다가 다음 체크 때 이어서 처리. 무한정 쌓이지 않도록 일정 기간(예: 3일) 이상 대기한 항목은 자동 정리
- **옵션 B (당일 우선순위만)**: 그날 가장 먼저 올라온 영상만 처리하고 나머지는 이월 없이 스킵
- 사용자가 로컬 웹 UI에서 두 옵션 중 선택 가능

**FR5. 이메일 발송 (기본이자 유일한 발송 채널, OAuth 불필요)**
- Gmail 앱 비밀번호(App Password) + SMTP로 본인 이메일에 발송, PDF 직접 첨부
- 사용자가 로컬 웹 UI에서 지정한 **발송 시각**(오전 6시~오후 6시, 1시간 단위 선택지, 기본값 오전 8:00)에 하루 1회, 그 사이 생성된 슬라이드들을 모아 "오늘의 요약" 형태 한 통으로 발송 (영상 제목, 채널명 포함)
- 발송 시각까지 새로 생성된 항목이 없으면 발송하지 않음
- 구글 OAuth·Cloud Console 설정이 전혀 필요 없음 (앱 비밀번호는 구글 계정 보안 설정에서 발급)

**FR6. (삭제됨) 카카오톡 발송**
- 검토 후 1차 버전에서 제외 확정. 사유는 DESIGN.md "검토 후 기각된 대안" 참고 (Drive OAuth 연동 필요성이 "Cloud Console 불필요" 원칙과 충돌)

**FR7. 채널 등록 개수 권장 안내**
- 최대 5개 이내 채널 등록을 권장하는 안내를 채널 등록 화면에 상시 노출 (현재 등록 수 함께 표시)
- 6개 이상 등록 시 "채널을 6개 이상 등록하면 슬라이드 생성과 발송이 느려질 수 있어요" 경고를 채널 등록 화면에 노출
- 하드 리밋은 아니며, 근거는 NotebookLM 무료 요금제의 확인된 Audio/Video Overview 한도(3회/일)에 기반한 추정치 (실제 한도는 구현 후 실측 필요)

**FR8. 로컬 웹 UI**
- PC에서 실행되는 로컬 웹 서버로 제공. 같은 네트워크 내에서는 모바일 브라우저로도 접근 가능 (단, 엔진 자체는 PC에서만 실행됨 — DESIGN.md "왜 엔진은 PC에만 있어야 하는가" 참고)
- 제공 화면
  - **시작하기**: 서비스 소개, 첫 진입 화면
  - **채널**: 채널 URL 등록/삭제, 등록 개수 안내(FR7) 및 쇼츠 자동 제외 안내
  - **발송 설정**: 이메일 주소, Gmail 앱 비밀번호(가이드형 "연결하기" 버튼 + 붙여넣기), 발송 시각, 대기열 정책(FR4)
  - **대시보드**: 등록 채널 수, 다음 발송 시각, NotebookLM 재로그인 필요 배너, 처리 실패 목록, 발송 대기함 현황

**FR9. 실행/스케줄링 ("따라잡기" 방식)**
- 정확한 시각에 강제 실행되는 방식이 아니라, **프로그램이 실행될 때마다 "밀린 일감이 있는지" 스스로 확인해서 처리**하는 방식 (다중 사용자 배포 시 "24시간 켜진 전용 기기 필요"라는 진입장벽을 낮추기 위한 결정, DESIGN.md 참고)
- **감지+생성**: 마지막 확인으로부터 1시간이 지났으면 즉시 확인·처리, 생성된 슬라이드는 즉시 발송하지 않고 "발송 대기함"에 저장
- **발송**: 사용자가 지정한 발송 시각이 지났는데 오늘 아직 발송 안 했으면 즉시 발송, 대기함에 쌓인 항목을 모아 이메일로 일괄 발송 후 대기함 비움
- 설치 시 "컴퓨터 시작 시 자동 실행" 등록 안내 (예정) — 사용자가 프로그램 실행을 따로 챙기지 않아도 평소 컴퓨터 사용 패턴에 맞춰 자연스럽게 동작하도록 함
- 로컬 웹 UI는 최초 설정과 이후 상시 관리를 위해 제공됨

## 7. 비기능 요구사항
- **비용**: 모든 구성 요소가 무료 티어 내에서 동작, 사용자에게 과금 발생 없음
- **진입장벽 최소화**: **어떤 사용자도 Google Cloud Console을 열 필요가 없음.** 채널 등록은 URL 붙여넣기, 이메일은 계정 보안 설정에서 발급하는 앱 비밀번호, 쇼츠 판별은 라이브러리(yt-dlp)로 전부 해결
- **사용성**: 개발 지식 없는 사용자도 README를 따라 설치 가능해야 함, 설정/관리는 터미널이 아닌 로컬 웹 UI로 진행 (FR8 참고)
- **신뢰성**: NotebookLM 세션 만료 등 자동화 실패 시 사용자가 알아챌 수 있는 장치 필요 (로컬 웹 UI 배너 + 이메일 알림, FR8 참고)
- **유지보수성**: 비공식 라이브러리(notebooklm-py, yt-dlp) 의존도가 있으므로, 해당 연동 부분을 모듈로 분리해 변경에 대응하기 쉽게 구성

## 8. Out of Scope (1차 버전 제외)
- 다중 사용자/중앙 서버 (각자 자기 인스턴스를 셀프호스팅하는 구조이므로 로그인 시스템·사용자 관리 없음)
- 모바일 네이티브 앱 (로컬 웹 UI는 모바일 브라우저로 열람 가능하나, 별도의 전용 앱은 제작하지 않음)
- 실시간 즉시 알림 (감지는 폴링 주기에 따른 지연 존재, 발송은 하루 1회 지정 시각으로 배치 처리됨)
- **카카오톡 발송** — 검토했으나 Drive OAuth 연동이 "Cloud Console 완전 배제" 원칙과 충돌해 1차 버전에서 제외 (DESIGN.md 참고)
- 유료 API 폴백(Gamma, 유료 프록시 등) — 무료 우선 원칙에 따라 1차 버전 제외
- 클라우드(GitHub Actions 등) 실행 — 자막 추출 등 유튜브 콘텐츠 접근이 클라우드 IP에서 차단될 위험 때문에 로컬 PC 실행만 지원

## 9. 성공 기준 (Acceptance Criteria)
- 사용자가 Google Cloud Console을 한 번도 열지 않고 README만 보고 설치를 완료할 수 있다
- 사용자가 로컬 웹 UI를 통해 채널 URL 등록, 이메일/발송 시각 설정을 완료할 수 있다
- 등록한 채널에 신규 롱폼 영상이 올라오면, 사용자가 지정한 발송 시각에 그날 생성된 슬라이드 요약이 "오늘의 요약"으로 이메일에 도착한다
- 생성된 PDF가 실제 영상 내용을 반영한 슬라이드로 구성되어 있다
- 자막이 없는 영상 1건이 (재시도 후에도) 실패해도 나머지 채널/영상 처리는 정상 진행된다
- 채널을 6개 이상 등록하면 채널 화면에 성능 저하 경고가 표시된다

## 10. 리스크 (요약, 상세는 DESIGN.md 참고)
- NotebookLM 자동화가 비공식이라 세션 만료·정책 변경에 취약
- `yt-dlp`도 비공식 라이브러리라 유튜브 페이지 구조 변경에 영향받을 수 있음 (다만 활발한 커뮤니티가 빠르게 대응)
- NotebookLM 무료 요금제의 일일 슬라이드덱 생성 한도가 공식적으로 공개되지 않아, 실제 운영 중 예상보다 자주 대기열이 밀릴 가능성 있음 (실측 필요)
- 로컬 PC 실행은 대체 불가 요건 — NotebookLM 자동화와 자막 추출 모두 클라우드 IP에서 차단 위험이 있어, GitHub Actions 등으로 이전 불가 (DESIGN.md "검토 후 기각된 대안" 참고)

## 11. 오픈 이슈
1. ~~최초 설치 UX~~ → **결정: 로컬 웹 UI 채택** (FR8)
2. ~~NotebookLM 세션 만료 알림 방법~~ → **결정**: 로그인 실패 감지 시 로컬 웹 UI 배너 + 이메일 알림 (FR8)
3. ~~자막 없는 영상 처리~~ → **결정**: 1회 재시도 후 최종 실패 처리 (FR3)
4. ~~폴링 주기 기본값~~ → **결정: 1시간** (최초 결정 6시간이었으나, "따라잡기" 스케줄링 방식으로 전환하며 PC를 억지로 깨울 필요가 없어져 단축)
5. ~~여러 영상 동시 업로드 시 처리~~ → **결정**: 대기열 정책(FR4)으로 해결
6. ~~채널 개수 제한~~ → **결정: 최대 5개 이내 권장, 6개 이상 시 경고 노출** (FR7). 정확한 한도는 실측 필요
7. ~~쇼츠 판별 방법~~ → **결정: yt-dlp 사용, API 키 불필요** (FR2)
8. ~~카카오톡 발송 여부~~ → **결정: 1차 버전에서 제외** (Cloud Console 완전 배제 원칙 우선)
9. **크롬 vs 파이어폭스 (미결, 실측 예정)**: notebooklm-py의 쿠키 기반 인증이 크롬 127+ 암호화로 막힐 가능성 있음. 구현 후 두 브라우저 모두 테스트해 최종 결정

## 12. 마일스톤 (대략적 순서, 일정 미정)
- M1: 로컬 웹 UI 실제 구현 (채널 등록, 발송 설정, 대시보드 화면을 실제 설정 파일과 연결)
- M2: yt-dlp 기반 신규 업로드 감지 + 쇼츠 필터링 (완료, 실제 채널로 검증) + NotebookLM 연동 파이프라인 (완료, 실제 영상 1개로 로그인→PDF 다운로드까지 전 과정 성공 확인, 상세는 DESIGN.md "NotebookLM 실제 연동 방식" 참고)
- M3: 발송 대기함 + 앱 비밀번호 SMTP 이메일 일괄 발송(PDF 첨부) — **완료, 실제 이메일 수신까지 확인함** (`src/sender.py`)
- M4: 대기열 정책 + 자막 재시도 + 세션 만료 알림 — **완료, 전부 실제 테스트/화면으로 확인함**
- M5: "따라잡기" 스케줄링 로직 구현(완료) + 컴퓨터 시작 시 자동 실행 등록(macOS 완료, Windows 예정) + README/설치 가이드 정리 후 오픈소스 공개
