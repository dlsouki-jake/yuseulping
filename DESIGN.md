# 유슬핑 (유튜브 슬라이드 브리핑)

## 한 줄 요약
구독 중인 유튜브 채널에서 새 롱폼 영상이 올라오면, NotebookLM으로 슬라이드 요약본(PDF)을 자동 생성해서 매일 지정한 시각에 이메일로 자동 발송하는 개인용 자가호스팅 도구.

## 배포 형태
- **개인용, 오픈소스(GitHub)**. 중앙 서버 없음.
- 필요한 사람이 각자 리포를 받아서, 각자 본인 이메일 계정으로 로컬(본인 PC)에서 실행.
- 서버 비용 0원 목표 — 전부 무료 도구/무료 티어만 사용.
- **구글의 심사(verification)나 결제, Cloud Console 설정이 전혀 없이, 누구나 무료로 설치·사용 가능**하도록 설계 (근거는 아래 "다중 사용자 배포와 OAuth 전면 제거" 참고)

## 전체 흐름 (사용자 시나리오)
1. 프로그램 실행 → 로컬 웹 UI가 브라우저에 열림
2. **요약받고 싶은 유튜브 채널의 URL을 직접 붙여넣어 등록** (구글 로그인/구독목록 자동조회 없음 — 채널은 최대 5개 이내 등록을 권장, 6개 이상 등록 시 "슬라이드 생성과 발송이 느려질 수 있어요" 경고 노출)
3. 받을 이메일 주소와 **Gmail 앱 비밀번호**(구글 계정 보안 설정에서 발급, OAuth·Cloud Console 아님)를 입력
4. 슬라이드 요약을 받아볼 발송 시각(오전 6시~오후 6시, 1시간 단위 선택, 기본 오전 8:00)을 지정
5. 이후 프로그램이 **1시간마다** 등록한 채널들의 신규 롱폼 업로드를 자동 감지 (쇼츠는 제외) — 단, 정확히 그 시각이 아니라 "컴퓨터가 켜져 있을 때 따라잡는" 방식 (아래 "실행 스케줄링 — 따라잡기 방식" 참고)
6. 신규 영상 발견 시 NotebookLM 자동화로 슬라이드 요약(PDF) 생성 → "발송 대기함"에 저장
7. 지정한 발송 시각에, 그날 쌓인 요약들을 "오늘의 요약"으로 묶어 이메일로 자동 발송 (PDF 직접 첨부)

## 아키텍처

```
watcher.py (컴퓨터가 켜져 있을 때 1시간 간격으로 "따라잡기" 실행)
├─ 채널별 최근 업로드 감지: yt-dlp로 채널 "동영상" 탭의 최근 N개 조회
│   (※ 원래 계획은 RSS 피드였으나, 실제로 확인해보니 유튜브 RSS 주소가 더 이상
│   응답하지 않아(404) yt-dlp 기반으로 교체함 — 아래 "결정 사항" 참고)
├─ 신규 영상 필터링
│   ├─ yt-dlp 라이브러리로 재생시간 조회 (API 키·OAuth·콘솔 설정 전혀 불필요)
│   │   → 180초(설정 가능) 이하면 쇼츠로 간주, 제외
│   └─ 자막이 아직 없으면(업로드 직후 자동자막 생성 중일 수 있음) 다음 주기에 1회 재시도 후 최종 실패 처리
├─ 대기열 관리 (NotebookLM 무료 요금제 일일 생성 한도 대응)
│   ├─ 옵션 A(기본): 한도 초과분은 대기열에 쌓아 다음 주기에 이어서 처리 (3일 이상 대기 시 자동 정리)
│   └─ 옵션 B: 당일 가장 먼저 올라온 영상만 처리, 나머지는 이월 없이 스킵
├─ notebooklm-py 라이브러리로 자동화
│   ├─ 새 노트북 생성 → YouTube URL을 소스로 추가
│   ├─ Slide Deck 생성 요청
│   └─ 완료되면 PDF로 다운로드
├─ 생성된 항목(PDF 포함)을 "발송 대기함"(로컬 DB/파일)에 저장 (즉시 발송하지 않음)
└─ NotebookLM 로그인 세션 만료 감지 시: 로컬 웹 UI에 재로그인 필요 배너 + 이메일로 중단 알림

sender.py (로컬 cron/스케줄러로 사용자 지정 시각에 1일 1회 실행, 기본 08:00)
├─ 발송 대기함에서 미발송 항목 전체 조회
├─ 항목이 없으면 발송하지 않고 종료
└─ 있으면 "오늘의 요약" 형태로 묶어서 이메일 발송
    └─ Gmail 앱 비밀번호 + SMTP로 발송, PDF 직접 첨부 (OAuth 불필요)
    → 발송 성공 후 대기함 비움(발송 완료 처리)
```

## 기술 스택
- **언어**: Python (notebooklm-py가 Python 라이브러리라 통일)
- **채널 등록**: 사용자가 채널 URL을 직접 입력 (OAuth·구독목록 조회 API 불필요). yt-dlp로 채널 이름을 자동 조회해 표시
- **신규 업로드 감지**: [`yt-dlp`](https://github.com/yt-dlp/yt-dlp)로 채널의 "동영상" 탭 최근 목록 조회 (`extract_flat`, 가볍게). RSS 피드는 실제로 끊긴 것으로 확인되어 폐기
- **쇼츠 판별**: 같은 yt-dlp로 영상 재생시간 조회. API 키·OAuth·Cloud Console 설정이 전혀 필요 없음 — `pip install`만으로 끝남. 깃허브 16만+ 스타, 월 1,200만+ 다운로드로 활발히 유지보수되는 프로젝트라 안정성도 높음
  - 실제 구현 중 발견: 유튜브 기본(web) 클라이언트로 영상 정보를 조회하면 "The page needs to be reloaded" 오류가 나는 경우가 있어, `extractor_args`로 android 클라이언트를 쓰도록 지정해 해결함
- **슬라이드 생성**: [`teng-lin/notebooklm-py`](https://github.com/teng-lin/notebooklm-py) — 비공식이지만 활발히 유지보수되는 라이브러리. YouTube 소스 추가 → Slide Deck 생성 → PDF 다운로드까지 지원 확인됨.
- **이메일 발송**: **Gmail 앱 비밀번호(App Password) + SMTP** — Google OAuth·Cloud Console 설정 불필요, 사용자 본인 구글 계정 보안 설정에서 발급, PDF 직접 첨부
- **스케줄링**: 클라우드/CI가 아닌 **로컬 PC**에서 실행. "정확한 시각에 실행"이 아니라 **"컴퓨터가 켜져 있을 때 밀린 일감을 따라잡는" 방식** (아래 "실행 스케줄링 — 따라잡기 방식" 참고). 감지는 1시간 간격, 발송은 1일 1회 지정 시각 기준
- **설정/관리 UI**: 로컬 웹 UI (PC에서 실행되는 미니 서버 + 브라우저 화면). 같은 네트워크 안에서는 모바일 브라우저로도 접근 가능. **단, 이 UI는 어디까지나 "PC에서 실행 중인 엔진을 들여다보는 창"이며, 엔진 자체를 폰에서 실행하는 게 아님** — 아래 "왜 엔진은 PC에만 있어야 하는가" 참고

## 다중 사용자 배포와 OAuth 전면 제거 (중요 결정 배경)

**문제 제기**: 오픈소스로 배포해서 "누구나 구글 로그인만 하면 바로 쓸 수 있게" 하려면, 개발자가 만든 **공유 OAuth 클라이언트 하나**로 모든 사용자가 로그인하는 구조가 이상적이지 않을까 검토함.

**리서치 결과, 공유 클라이언트 방식은 기각**:
- 구글 앱 검증(verification)을 받지 않은 "테스트" 상태 앱은 **테스트 사용자 100명 제한**이 있고, 등록 안 된 사용자는 로그인 자체가 거부됨
- 더 치명적으로, **테스트 상태에서 민감 스코프(`youtube.readonly`, `gmail.send`)를 요청하면 refresh token이 7일마다 만료**됨 → "한 번 설정하면 계속 자동으로 돈다"는 전제가 깨질 위험이 있었음
- 검증을 통과해 "게시(Production)" 상태로 전환하려면 **2~6주 심사 기간, 약 $540~$1,000 비용 추정**이 필요함 — 개인 무료 오픈소스 프로젝트에 비현실적
- "Internal(조직 내부)" 앱으로 우회하는 방법은 Google Workspace 조직 전용이라 일반 개인 사용자 대상 서비스엔 적용 불가

**1차 해결책(민감 스코프 제거)에서 → 최종적으로 OAuth 자체를 완전히 제거**:
- `youtube.readonly` 제거 → 구독 목록 자동조회 대신 **채널 URL 수동 입력**으로 전환
- `gmail.send` 제거 → Gmail API 대신 **앱 비밀번호(App Password) + SMTP**로 전환
- 애초에 `drive.file`(비민감 스코프)을 쓰려던 이유는 **카카오톡 발송 시 파일 첨부가 안 돼서 링크가 필요**했기 때문이었는데, **카카오톡 기능 자체를 제외**하기로 결정하면서 Drive 연동도 통째로 불필요해짐
- 쇼츠 판별용으로 검토했던 YouTube Data API "키" 발급도, 실제로 사용자가 직접 발급을 시도해보니 "프로젝트 생성 → API 활성화 → 사용자 인증정보 생성"까지 여러 단계라 예상보다 번거로움이 확인됨 → **`yt-dlp` 라이브러리로 대체, API 키도 완전히 제거**
- **결과: 지금 설계는 어떤 사용자든 Google Cloud Console을 단 한 번도 열 필요가 없음.** 딱 두 가지만 하면 끝: (1) 채널 URL 붙여넣기, (2) 구글 계정 보안 설정에서 앱 비밀번호 발급

## NotebookLM 실제 연동 방식 (실측으로 확정된 내용)

M2 개발 중 실제로 로그인부터 슬라이드 다운로드까지 전체 파이프라인을 라이브 테스트해서 확정한 내용.

**1. `notebooklm-py`의 기본 방식(쿠키 추출 후 재사용)은 실제로 막혀 있었음**
- 이 라이브러리는 Playwright로 로그인시킨 뒤 그 쿠키를 꺼내서, 완전히 별도인 httpx 프로그램으로 재사용하는 구조
- 실제로 테스트해보니 **구글이 최근 "로그인 세션을 그 브라우저에 묶어버리는" 보안(디바이스 바운드 세션)을 적용**한 것으로 보임 — 같은 쿠키를 진짜 브라우저로 쓰면 정상 작동하지만, 쿠키만 빼서 다른 프로그램(httpx)으로 재사용하면 전부 로그인 페이지로 튕겨나감 (실측 확인)
- 즉 **쿠키 추출·재사용이라는 접근 자체가 구글의 최신 보안 정책과 근본적으로 충돌**함

**2. 해결책 — "살아있는 로그인된 브라우저 안에서 직접 호출"로 우회**
- 로그인이 끝난 뒤 브라우저를 끄지 않고 계속 띄워둔 채로, `page.evaluate()`를 이용해 그 브라우저의 자바스크립트 안에서 직접 `fetch()`를 호출함 (사람이 개발자도구 콘솔에서 API를 직접 두드리는 것과 같은 효과) → 브라우저에 묶인 세션이 자동으로 함께 전달돼서 정상 작동함 (실측 확인)
- 요청 주소가 구글의 옛 도메인(`notebooklm.google.com`)으로 하드코딩돼 있어서 그대로 쓰면 실제 접속 도메인(`notebook.google.com`)과 달라 브라우저가 교차 출처(cross-origin) 요청으로 막아버림 → 도메인을 실제 접속 주소로 바꿔서 해결
- **파일(PDF) 다운로드는 다운로드 대상 도메인이 또 다르고(`contribution.usercontent.google.com`) 교차 출처라 `fetch()`로도 막힘** → Playwright의 `context.request.get(url)`을 사용해 해결 (브라우저 JS 실행이 아니라 Playwright 자체의 요청 기능이라 브라우저의 CORS 제약을 받지 않으면서도 같은 브라우저 세션의 쿠키를 그대로 씀)
- 이 세 가지(RPC 호출 브라우저 경유, 도메인 교정, 다운로드는 context.request 사용)를 적용해서 **로그인 → 노트북 생성 → 소스 추가 → 슬라이드 생성 → PDF 다운로드까지 전체 과정을 실제로 성공시킴**

**3. 라이브러리 자체의 버그 발견 및 우회**
- `notebooklm-py`의 `wait_for_completion()`이 아티팩트 상태가 "대기 중(PENDING=2)"인데도 "완료"로 잘못 판단하는 버그를 실측으로 발견함 (내부 폴백 로직이 `status_code == 1`이 아니면 무조건 완료로 취급)
- 이 버그를 우회해서, 상태 코드가 정확히 "완료(COMPLETED=3)"일 때까지 직접 폴링하는 로직을 자체 구현함 (`src/slides.py`의 `_wait_for_slide_deck`)

**4. 실제 소요 시간 (중요, 실측)**
- 슬라이드덱 하나 생성에 **최대 30분 가까이 걸릴 수 있음이 실측으로 확인됨** (영상 길이 약 6분 기준). 기존에 예상했던 "몇 분"보다 훨씬 오래 걸림
- 이 때문에 처리 파이프라인의 타임아웃 값들을 넉넉하게(예: 40분) 잡아둠

**5. 요약**: `notebooklm-py`는 "요청 만들기/응답 해석하기" 로직은 그대로 재사용하되, 실제 네트워크 요청은 저장된 로그인 세션을 가진 Playwright 브라우저를 경유하도록 직접 재구성해서 씀 (`src/slides.py` 참고). 완전히 새로 만든 게 아니라 라이브러리를 "부분적으로 우회"한 형태.

## 왜 엔진은 PC에만 있어야 하는가 (모바일/클라우드 실행 불가 이유)
- 신규 업로드 감지, 이메일 발송, 쇼츠 판별은 전부 가벼운 API/라이브러리 호출이라 실행 위치에 제약이 없음 (폰이든 클라우드든 동일)
- 그러나 **NotebookLM은 공식 API가 없어서**, `notebooklm-py`가 실제 **데스크톱 브라우저 프로그램 전체를 자동으로 조종**(버튼 클릭 흉내)해서 슬라이드를 생성함. 이건 API 호출이 아니라 데스크톱급 브라우저 실행 자체가 필요한 작업
- 모바일 OS(iOS/Android)는 이런 식의 상시 백그라운드 브라우저 자동화를 지원하지 않음 (기술적 제약 + 앱스토어 정책)
- 클라우드(GitHub Actions 등)로 옮기는 것도 기각됨 — 자막 추출 등 유튜브 콘텐츠 접근 단계가 데이터센터 IP에서 오히려 더 잘 차단됨 (아래 "검토 후 기각된 대안" 참고)
- 따라서 "설정 화면(로컬 웹 UI)"은 모바일 브라우저로 열람 가능하지만, "엔진(감지+생성+발송)"은 반드시 데스크톱 브라우저를 실행할 수 있는 PC에 있어야 함
- **모바일 앱으로 전환하는 방안도 검토했으나 기각**: 이유는 인터넷 연결 여부가 아니라, iOS/Android가 일반 앱의 무기한 백그라운드 실행 자체를 허용하지 않기 때문 (배터리·개인정보 보호 목적의 OS 정책). NotebookLM 자동화(데스크톱 브라우저 필요)를 걷어내도, "몇 시간마다 알아서 확인하고 발송하는" 백그라운드 상시 실행 자체가 모바일 앱 생태계에서는 지원되지 않음

## 실행 스케줄링 — "정확한 시각"이 아니라 "따라잡기" 방식

**문제 제기**: 배포 대상이 개발자 본인이 아니라 불특정 다수라서, "전용 미니PC/라즈베리파이를 마련해서 24시간 켜두세요"라는 요구는 사용률을 크게 떨어뜨릴 것이라는 지적이 있었음. 실제로 일반 사용자에게 상시 가동 전용 기기를 요구하는 건 비현실적.

**해결책 — 스케줄링 방식 자체를 변경**:
- 기존 방식(폐기): "정확히 6시간마다/정확히 지정 시각에 실행"을 전제로 한 cron 스케줄 — 그 시각에 PC가 꺼져 있으면 그냥 놓침
- **새 방식**: 프로그램이 실행될 때마다(예: 컴퓨터 켤 때 자동 실행되도록 등록) "마지막 확인으로부터 1시간이 지났나?", "오늘 발송 시각이 지났는데 아직 발송 안 했나?"를 스스로 확인해서, **밀린 일감이 있으면 그 자리에서 바로 처리**함
- 이 방식이면 하루에 한 번이라도 컴퓨터를 켜는 사용자는 문제없이 쓸 수 있음 — "24시간 켜둬야 한다"는 진입장벽이 사라짐
- 이 방식으로 바뀌면서, PC를 억지로 깨울 필요가 없어져 **감지 주기를 6시간 → 1시간으로 단축**함 (배터리 부담 우려가 없어졌고, 확인 자체는 원래 비용이 거의 안 드는 작업이라 더 자주 해도 무방)
- 설치 시 "컴퓨터 시작 시 자동 실행" 등록을 안내해서, 사용자가 따로 프로그램 실행을 신경 쓰지 않아도 평소 컴퓨터 사용 패턴에 자연스럽게 묻어가도록 함 (구현 예정)

## 채널 등록 개수 권장 기준
- NotebookLM 무료 요금제의 Audio/Video Overview 확인된 한도(3회/일)를 근거로, Slide Deck도 유사 수준일 것으로 추정
- **최대 5개 이내 채널 등록을 권장**, 6개 이상 등록 시 로컬 웹 UI에 "슬라이드 생성과 발송이 느려질 수 있어요" 경고 노출
- 하드 리밋은 아니며, 정확한 한도는 실제 구현 후 실측 필요 (오픈 이슈로 남아있음)

## 알려진 리스크 / 제약사항
1. **NotebookLM 자동화는 비공식**(공식 API 아님, `notebooklm-py`가 내부적으로 undocumented RPC 호출)
   - ~~Chrome 127+ 쿠키 암호화로 파이어폭스가 필요할 수 있다~~ → **실제 코드 확인 결과 사실이 아님(정정)**: 이 라이브러리는 브라우저의 저장된 쿠키를 직접 읽는 게 아니라, **Playwright**로 별도의 독립된 브라우저 프로필을 띄워 로그인시키고 그 결과(`storage_state.json`)를 저장하는 방식이라 크롬/파이어폭스 구분 자체가 의미 없음. 로그인은 `scripts/notebooklm_login.py`(최초 1회, 사용자가 직접 실행) 담당
   - 로그인 세션이 1~2주마다 만료 → 주기적으로 수동 재인증 필요 (완전 무인 자동화는 어려움)
   - 구글의 정책/UI 변경에 취약 (라이브러리가 깨질 수 있음)
   - **패키지의 공식 CLI(`notebooklm login` 등)는 Python 3.10+ 문법(`str | None`)을 써서 이 프로젝트 환경(Python 3.9)에서 import 자체가 실패함** → CLI 대신 라이브러리(`NotebookLMClient`)를 직접 코드에서 불러와 쓰는 방식으로 우회함 (`src/slides.py`, `scripts/notebooklm_login.py`). Python을 3.10 이상으로 올리면 이 우회가 필요 없어짐 (추후 개선 여지)
   - **패키지 기본 방식(쿠키 추출 후 재사용)이 구글의 최신 보안(디바이스 바운드 세션으로 추정)에 막혀있음을 실측으로 발견, 브라우저 경유 호출로 우회함** (상세: 위 "NotebookLM 실제 연동 방식" 섹션)
   - **슬라이드덱 하나 생성에 최대 30분 가까이 걸릴 수 있음(실측)** — 예상보다 훨씬 느림. 여러 영상을 처리해야 하면 상당한 시간이 걸릴 수 있어, 대기열/일일 한도 정책과 맞물려 실사용 시 체감 속도에 유의 필요
2. **`yt-dlp`도 비공식 라이브러리** — 유튜브 페이지 구조가 바뀌면 일시적으로 깨질 수 있음. 다만 활발한 커뮤니티가 빠르게 대응하는 편이라 리스크는 상대적으로 낮음
3. **자막 없는 영상**은 NotebookLM이 처리 못 할 수 있음 → 업로드 직후엔 유튜브 자동자막이 아직 생성 중일 수 있어 1회 재시도 후 스킵 (해결됨)
4. **NotebookLM 무료 요금제 일일 생성 한도**: Audio/Video Overview 각 3회/일로 확인됨. Slide Deck 전용 한도는 구글이 비공개, 유사 수준(하루 한 자릿수)으로 추정. 채널 개수 자체보다 "일일 총 신규 영상 처리량"이 실질 제약 → 실제 구현 후 테스트로 실측해 대기열/경고 로직에 반영 예정
5. 유튜브/구글 어느 쪽도 이 자동화를 "공식 지원"하지 않으므로, 각 서비스의 이용약관을 개인적으로 검토하고 사용할 책임은 사용자 본인에게 있음

## 결정 사항 요약 (히스토리)
- ~~자막 추출 → LLM 요약 → Gamma API~~ (초기안, 폐기) → **NotebookLM 자체 자동화**로 전환
- 이메일은 **기본이자 유일한 발송 채널** (카카오톡은 최종적으로 제외, 아래 참고)
- **다중 사용자 배포를 위해 Google OAuth를 전면 제거**: 채널 URL 수동 입력 + 앱 비밀번호(SMTP) + yt-dlp로 대체 → 어떤 사용자도 Google Cloud Console을 열 필요 없음
- 서비스명 확정: **유슬핑** (유튜브 슬라이드 브리핑)
- 채널 등록 개수: **최대 5개 이내 권장**, 6개 이상 시 UI 경고 노출
- 발송 시각: **오전 6시~오후 6시, 1시간 단위 선택** (기본 오전 8시)
- 로컬 웹 UI 화면 목업 제작 완료 (4개 화면: 시작하기/채널/발송 설정/대시보드)
- M1(웹 UI)~M3(이메일 발송)까지 실제 계정으로 전체 파이프라인 성공 확인 (로그인→노트북 생성→슬라이드 생성→PDF 다운로드→이메일 발송)
- **실행 스케줄링을 "정확한 시각" 방식에서 "따라잡기" 방식으로 변경**: 다중 사용자 배포 시 "24시간 켜진 전용 기기 필요"라는 진입장벽을 낮추기 위함. 이에 맞춰 감지 주기도 6시간 → **1시간**으로 단축

## 검토 후 기각된 대안 (재검토 방지용 기록)
1. **GitHub Actions(클라우드)로 실행 이전** → 기각. 자막 추출 등 유튜브 콘텐츠 접근 단계가 클라우드/데이터센터 IP에서 오히려 더 잘 차단됨. 무료 우회는 사실상 불가능.
2. **NotebookLM → Gemini API 텍스트 요약으로 교체** → 기각 (사용자가 실제 슬라이드 PDF 유지를 선택). Gemini API는 텍스트만 생성하고 시각적 슬라이드 PDF를 만들지 않음.
3. **카카오톡 → 텔레그램 봇으로 교체** → 기각 당시엔 채택 안 함(카카오 유지 결정) → **이후 카카오톡 기능 자체를 최종 제외**로 결론이 바뀜. 사유: 카카오톡 발송에 필요한 Google Drive OAuth 연동이 "이메일은 콘솔 불필요"라는 원칙과 충돌 + 사용자가 직접 API 키 발급을 시도해보니 예상보다 번거로움을 확인 → 배포 편의성을 우선해 카카오톡 기능 전체를 1차 버전에서 제외하기로 결정
4. **Gmail App Password로 OAuth 대체** → ~~최초 기각~~ (당시 사유: "어차피 Drive 때문에 OAuth 필요") → **이후 재검토하여 채택**. 다중 사용자 배포 목표 하에서 Google 앱 검증 절차를 피하기 위한 핵심 수단으로 재평가됨.
5. **개발자가 만든 공유 OAuth 클라이언트로 검증 없이 다수 사용자 지원** → 기각. 테스트 모드 100명 제한 + 민감 스코프 refresh token 7일 만료 + 검증 비용/기간이 개인 오픈소스 프로젝트에 비현실적.
6. **YouTube Data API 키로 쇼츠 판별** → 기각. 개념상으론 심사가 필요 없는 가벼운 절차였지만, 실제로 발급을 시도해보니 Cloud Console에서 프로젝트 생성 → API 활성화 → 사용자 인증정보 생성까지 여러 단계를 거쳐야 해서 비개발자에게는 여전히 허들이었음 → **`yt-dlp` 라이브러리로 대체, 사용자 설정 단계 자체를 완전히 제거**
7. **Google Drive 연동(카카오톡용)** → 카카오톡 기능 제외와 함께 자동으로 불필요해짐 (더 이상 필요한 곳이 없음)
8. **PC 상시 켜짐 문제 해결을 위해 모바일 앱으로 전환** → 기각. 원인이 인터넷 연결이 아니라 iOS/Android가 일반 앱의 무기한 백그라운드 실행을 허용하지 않는 OS 정책이라, NotebookLM 자동화를 걷어내도 근본적으로 해결 안 됨. 대신 "따라잡기" 스케줄링 방식으로 PC 상시 켜짐 요구사항 자체를 완화하는 쪽으로 해결

## 다음 단계
- [x] **로컬 웹 UI 실제 구현** — Flask 앱으로 채널 등록/삭제, 발송 설정 저장, 대시보드(실제 채널 수 반영)까지 실제로 동작 확인함. `src/channels.py`(yt-dlp로 채널 정보 조회), `src/config_store.py`(config.yaml 읽기/쓰기), `src/webui/app.py` + `templates/*.html` + `static/style.css`. 예전 OAuth 기반 코드(`auth.py`, `youtube_api.py`)는 삭제함
- [x] **신규 업로드 감지 + 쇼츠 필터링** — `src/recent_uploads.py`(yt-dlp), `src/state_store.py`(중복 방지), `src/watcher.py`. 실제 채널(MKBHD)로 테스트해서 신규 영상 5개 정확히 감지, 재실행 시 중복 없이 스킵되는 것까지 확인함
- [x] **NotebookLM 슬라이드 생성 — 로그인부터 PDF 다운로드까지 실제 전체 성공 확인함** — `src/slides.py`, `scripts/notebooklm_login.py`. 쿠키 재사용 방식이 막혀있는 것을 발견해 "브라우저 경유 호출"로 우회했고, 라이브러리의 완료 판단 버그도 발견해 직접 폴링으로 우회함 (상세: 위 "NotebookLM 실제 연동 방식" 섹션). 실제 유튜브 영상 1개로 노트북 생성 → 소스 추가 → 슬라이드 생성 → PDF 다운로드까지 전 과정 성공, 실제 12페이지 PDF 파일 생성 확인함
- [x] **이메일 발송 — 실제 발송·수신까지 확인함** — `src/sender.py`. Gmail 앱 비밀번호 + SMTP(587, STARTTLS)로 PDF 첨부 이메일 발송, 발송 성공 후 대기함 비우는 것까지 실제 계정으로 테스트 완료
- [x] **README.md 전면 재작성 완료** — 구버전 OAuth 안내 삭제, 실제 설치 순서(venv → NotebookLM 로그인 → 웹 UI → 자동 실행 등록)로 새로 작성. 앱 비밀번호 발급 안내도 발송 설정 화면 자체 가이드로 대체됨(FR9 참고)
- [x] **대기열 정책(carry_over/daily_drop) 실제 구현 — 가짜 데이터로 전 시나리오 테스트 완료** — `state_store.py`(`queued`/`generated_count`/`remaining_daily_budget`/`expire_stale_queue_items`), `watcher.py`(`process_queue`). 예산 초과 시 이월 vs 당일드랍 두 정책 다 검증, 3일 지난 대기 항목 자동 정리도 확인
- [x] **자막 없는 영상 fallback 정책 구현 — 대기열 구현과 함께 완료, 테스트 완료** — 생성 실패 시 `retry_count`로 1회만 재시도 후 최종 실패(`failed` 목록) 처리. 가짜 실패로 재시도→최종실패 전환까지 확인
- [x] **NotebookLM 로그인 만료 감지 — 대시보드 배너 + 이메일 알림까지 실제 화면으로 확인** — `slides.py`(`NotebookLMAuthError`), `watcher.py`(`handle_login_expired`, 큐 보존), `sender.py`(`send_alert_email`), `notebooklm_login.py`(재로그인 시 플래그 해제), 대시보드(`app.py`+`dashboard.html`+배너/칩 CSS). 가짜 상태로 배너·최근 처리 내역(완료/재시도/실패 칩) 렌더링까지 스크린샷으로 확인
- [ ] NotebookLM 일일 생성 한도 실측 (지금은 추정치 3개/일을 `daily_generation_limit`로 사용 중, 실측 후 조정 필요)
- [ ] (선택) Python 3.9 → 3.10+ 업그레이드 검토 (notebooklm-py 공식 CLI 사용 가능해짐)
- [x] **"따라잡기" 스케줄링 로직 구현 — 실제 테스트 완료** — `state_store.py`에 `last_checked_at`/`last_sent_at` 기록 + `should_check_now()`/`should_send_now()` 판단 함수 추가. `watcher.py`/`sender.py`가 실행될 때마다 이걸로 필요 여부를 스스로 판단(둘 다 `--force`로 강제 실행 가능). 처음 실행/너무 빨리 재실행/강제 실행 3가지 시나리오 다 테스트해서 정상 동작 확인
- [x] **컴퓨터 시작 시 자동 실행 등록(macOS) — 실제 등록·자동 실행까지 확인함** — `scripts/install_autostart.py`(launchd LaunchAgent 등록, RunAtLoad + 1시간마다 StartInterval), `scripts/run_pipeline.sh`(watcher→sender 순차 실행). 실제로 등록해서 로그인 시점 자동 실행이 되는 것, 로그 파일에 실행 흔적 남는 것까지 확인함
- [ ] Windows용 자동 실행 등록 (작업 스케줄러) — 아직 미구현, macOS(launchd)만 있음
